# 运行时数据：库、密钥、备份。绝不入库。
data/
*.db
*.db-wal
*.db-shm
*.key

# 环境配置：含真实上游 key
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# 编辑器
.vscode/
.idea/

# 局域网演示用的临时 Caddy 配置
.lan-demo/

# 网关的二进制与导出的根证书。**二进制不进版本库**：45 MB，且每台机器的
# 平台/架构不同——用 ./deploy/edge/edge get 现取（见 deploy/edge/CADDY.md）。
deploy/edge/caddy
deploy/edge/caddy.exe
deploy/edge/root.crt
