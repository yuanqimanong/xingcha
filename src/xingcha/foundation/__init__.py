"""跨层基础件：密钥环与错误信封。

这两样谁都要用（api / core / services / web 全都 import 它们），所以**不属于任何一层**
——放进某一层就会逼出反向依赖，而分层断言正是为了挡住那个。放在包根又会让根目录变成
"装配点 + 杂项"混在一起的地方，于是单独一个目录，名字直说它是什么。

允许 import 契约层，不许 import 其它任何层。这条由 ``tests/test_layering.py`` 守着。
"""
