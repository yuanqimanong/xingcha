# 星槎部署配置。复制成仓库根目录的 .env 后填写。
#
# 这个文件会被 deploy.sh 自动复制过去。.env 本身不入版本库。

# ---------------------------------------------------------------------------
# 必填
# ---------------------------------------------------------------------------

# 已解析到本机的域名。Caddy 用它申请证书，星槎用它生成 base_url 示例。
XINGCHA_DOMAIN=xc.example.com

# ---------------------------------------------------------------------------
# 证书
# ---------------------------------------------------------------------------

# 证书到期提醒邮箱。留空也能签发，但出问题时收不到通知。
ACME_EMAIL=

# 首次上线**先用 staging 走一遍**确认链路（DNS、80 端口、防火墙都对），
# 再注释掉这一行切正式 CA。
#
# 直接上正式 CA 时，一次配置错误就可能撞上 Let's Encrypt 的速率限制，然后要等一周。
# staging 签出来的证书浏览器会报不受信任——那是预期的，只用来验证链路。
#
# ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory

# ---------------------------------------------------------------------------
# 可选
# ---------------------------------------------------------------------------

# DEBUG / INFO / WARNING / ERROR
XINGCHA_LOG_LEVEL=INFO

# 上游 key **不要长期放在这里**。
#
# 环境变量会出现在 `docker inspect` 与 `/proc/<pid>/environ` 里。星槎只在首次启动时
# 把它加密导入数据库并打一条告警，之后永久忽略——导入成功后请从这个文件里删掉。
#
# 更好的做法是启动后在后台的「设置」页填写，或者：
#   docker compose exec xingcha xingcha config set openrouter.api_key -
#
# XINGCHA_OPENROUTER_API_KEY=

# 给**直通路径**也执行配额。默认关。
#
# 打开是一次**收紧**：原本能跑的直通请求会开始收到 429。所以做成显式开关而不是
# 升级的副作用；打开后 /version 的 features 会多一项 quota_passthrough，
# 调用方能探测到。
#
# XINGCHA_QUOTA_ON_PASSTHROUGH=1

# 真流式的 delta 合并窗口（秒）。默认 0.05。
#
# 不合并（设 0 是非法值，要不合并得改代码默认值）的话，一次长回答会产生上千个 SSE
# 帧，每帧一次 JSON 序列化加一次 send——在 1GB 的机器上是实打实的开销，而人眼分不出
# 0.05s 的差别。想要更"跳字"的观感就调小。
#
# XINGCHA_STREAM_DEBOUNCE_SECONDS=0.05

# ---------------------------------------------------------------------------
# 可观测（OTel → Langfuse）
# ---------------------------------------------------------------------------
#
# 上报地址与 Langfuse 凭据**不在这里**，在后台「设置 → 可观测」页填，或者：
#   docker compose exec xingcha xingcha config set trace.endpoint -
#   docker compose exec xingcha xingcha config set trace.public_key -
#   docker compose exec xingcha xingcha config set trace.secret_key -
#
# 理由和上游 key 一样：环境变量会出现在 docker inspect 与 /proc/<pid>/environ 里。
# **没配上报地址就是关闭**，这是默认状态。

# trace 里是否包含提示词与模型输出。默认 1（包含）。
#
# 关掉之后 trace 只有耗时、token、状态——"提示词改了一版为什么变差"这个问题就回答
# 不了了，而那是开 trace 的主要理由。指向别人家的托管服务时才值得考虑关掉。
#
# XINGCHA_TRACE_INCLUDE_CONTENT=0

# 上报时用的服务名。多环境共用一个 Langfuse 项目时靠它区分。
# XINGCHA_TRACE_SERVICE_NAME=xingcha
